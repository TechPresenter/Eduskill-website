# Storage is private. Never serve anything under /storage over the web.
# Backups, temp exports and other server-only files live here.
Require all denied
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
Options -Indexes
